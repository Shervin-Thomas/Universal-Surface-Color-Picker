# Universal-Surface-Color-Picker
It is an Editor tool (Script) that is used to detect and apply the surface color of any object with texture. It detects and applies the exact color in the texture that may contain multiple colors.
